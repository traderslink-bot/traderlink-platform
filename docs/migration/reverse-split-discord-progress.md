# Reverse-split implementation progress

Controlling [plan](reverse-split-discord-plan.md). Status: private full-feature source checkpoint ready for technical review; production remains the earlier owner-only empty preview. Full feature not activated or accepted.

## Registered-manifest and erasure proof — September 25, 2026

- Coordinator reviewed immutable cumulative `30495ce..a2f1ccf`, independently verified schema checksum `4d88aa953849d78e82b1d8d26c700da0b128f80678f3721b1e942dbd221f3f7d`, and cleared local-only registration of unchanged 0142. Added exactly one import, one ordered manifest file entry after 0140 and seven managed table names. All earlier entries/checksums unchanged; paused Welcome0141 remains excluded. No production application or configuration permission was implied.
- Added one owned `registered-migration.test.ts` integration case using a fresh in-memory SQLite database and the real manifest, migration runner, completed-database verifier and `PlatformErasureService`. Applied the predecessor, saved its entire registry, then applied only 0142; verified the predecessor registry is byte-for-value unchanged and a repeat migration run is a no-op.
- Seeded two synthetic users/workspaces, two preferences, four user deliveries and shared source/event/runtime/digest/Discord records. Erased only the first synthetic user through the real service. Their preference and deliveries disappeared; the second user's preference/two deliveries and all shared rows were preserved exactly. `foreign_key_check` was empty; full completed-schema verification matched before and after erasure. No shared erasure implementation changed and no private filesystem purge hook was supplied.
- Bounded run passed **1 file / 1 integration test** in 7.57 seconds (5.91 seconds in test), one fork worker, no file parallelism, 512 MB parent heap. Combined with the prior focused batch: 59 passing test cases. Only disposable in-memory data was created/deleted; no real user, application database, credential, provider request or notification was touched.
- Final registration static checks: targeted TypeScript 37 roots plus resolved dependencies passed with zero diagnostics (768 MB heap); lint both changed/new TypeScript files passed with zero findings (512 MB heap); whitespace check passed. Hosted predecessor/backup/restore/schema-compatible recovery, deployment/activation, actual populated UI and message/device receipts remain coordinator-controlled and unverified.

## Private notifications and runtime checkpoint — September 25, 2026

- Immutable implementation checkpoint `1faf1217734e5811f9e1057b6659b7e6aaaba890`, parent `f71c247242805aacd901f85ecabd4c7736340787`, sent to the Visible release coordinator with its full 24-file allowlist for schema/lifecycle review. No manifest approval or production action is implied.
- Follow-up acceptance batch: added digest planning/read-only preview/material correction tests, runtime enablement/schema/overlap/cleanup tests and existing-transport adapter target/idempotency/expiry tests. Final focused run on this source state: **6 files / 58 tests passed**, one fork worker, file parallelism disabled, 512 MB parent heap; duration 3.78 seconds. Final TypeScript 36 roots plus dependencies: zero diagnostics. Follow-up lint six changed/new files: zero findings. No broad suite/build/server/install.
- Bounded live read-only source checks: Nasdaq discovery returned ten reverse-split notices. [FBGL](https://www.nasdaqtrader.com/TraderNews.aspx?id=ECA2026-680) parsed 1-for-10 and [DLXY](https://www.nasdaqtrader.com/TraderNews.aspx?id=ECA2026-679) 1-for-5, both September 28 trading dates. [IMMP](https://www.nasdaqtrader.com/TraderNews.aspx?id=ECA2026-681) initially deferred because depositary conversion ratios appeared beside its explicit 1-for-20 split. Narrowly separated that ADS conversion clause; live recheck confirmed 1-for-20 / September 28. Parser version advanced to `2026-09-25.3`.
- SEC recent discovery for September 23–25 returned 70 hits, 69 eligible documents and one unresolved result, with no further page. This is document discovery, not 69 distinct approved splits. Three retrieved exhibits: [MRMD meeting material](https://www.sec.gov/Archives/edgar/data/1522767/000152276726000188/adef14aex99x1specialmeetin.htm) and [CDT certificate](https://www.sec.gov/Archives/edgar/data/1896212/000149315226044248/ex3-1.htm) conservatively deferred, each with one automatic related-document link. [CDT announcement](https://www.sec.gov/Archives/edgar/data/1896212/000149315226044248/ex99-1.htm) exposed a missed fixed board-selected ratio and a longer trading-date clause. Added those bounded forms plus conditional-approval regression coverage; live recheck confirmed 1-for-25 / September 29, not the September 28 legal effective time. Shareholder approval date remains unknown, not fabricated. No downloaded document or sample observation was inserted into an application database; no EODHD token was read or provider quote requested in this follow-up.
- Remaining gate: coordinator review/manifest and schema-compatible release/activation, actual Railway source collection and populated private UI, initial backfill/automatic approval coverage, actual private Discord/Push/email receipts and owner phone confirmation. Automated tests and these small source samples do not establish complete coverage or end-to-end production readiness.

- Owner approved full private operation plus separate Push/Email switches on Reverse Splits and Account → Preferences → Notifications. Implemented one shared client control and authenticated per-channel transactional action over the same default-off preferences; existing notification settings untouched. Added read-only Discord post preview and aligned the private Help guide. Added controls have not yet been rendered/visually accepted.
- Removed only the forced-empty source lock behind the two private enablement flags; existing exact owner page/metadata/API/navigation gates remain. Added coordinator-approved disabled dynamic worker hook with overlap guards, DB cleanup, fair collection and independently planned canonical digests. Neither flags nor production configuration changed.
- Expanded reserved 0142 source to seven News tables, adding immutable canonical digests, default-off user preferences and a recipient outbox. Digest/user FKs use RESTRICT; target identity is polymorphic and checked against existing active subscriptions/confirmed addresses. Final manifest registration, existing user-erasure compatibility, migration-verifier/backup rehearsal and production application remain coordinator technical gates. SQL checksum: `4d88aa953849d78e82b1d8d26c700da0b128f80678f3721b1e942dbd221f3f7d`.
- Reused Platform Web Push service/encryption and confirmed-email/Resend transport. Recipients require configured owner identity and explicit opt-in at enqueue and claim; target activity/confirmation is rechecked immediately before sending. Stable IDs, fenced leases, five-attempt limits, ordered revisions and midnight expiry prevent ordinary duplicate processing/stale replay. Email has provider idempotency; Push has a stable tag but an ambiguous network failure can still retry, so exactly-once device receipt is not promised. No existing halt/PR queue or subscription is mutated.
- Saved bot-specific View Channel, Send Messages and Read Message History Allow for existing TradersLink Platform in private channel `1552850262966669352`. Preserved @everyone View deny and all other overwrites. Coordinator GET-only proof at 2026-09-25T15:39:25.597Z: correct bot/guild/channel, all three permissions true, administrator false. Existing Top Admin/MEE6 admin bypass remains; Top Admin membership was not enumerated. No message sent and no credentials/OAuth changed.
- Owner explicitly authorized focused tests. Serial Vitest 4.1.4 runs using existing dependencies, one fork/worker, 512 MB parent heap: source contracts 27/27, Discord outbox/transport 10/10, notification preferences/recipients/recovery 6/6. All 43 passed. Tests use synthetic data and in-memory SQLite; no live database/provider/send. This verifies the authored SQL in fixture databases, not final production manifest/application or phone receipt.
- Targeted lint: 34 files, zero findings; only dependency-location React detection notice. Targeted TypeScript: 33 roots plus resolved dependencies, zero diagnostics, no emit and 768 MB heap cap. Whitespace check passed. No full regression/build, dependency install, local server or hosted action. Source/provider coverage, restart collection, populated UI, actual Discord/Push/email delivery and owner phone receipt remain unverified.

Earlier entries below are historical checkpoints; the current section supersedes their missing-channel, rejected-hook, absent-preferences and no-test statements.

## Completion work resumed — September 25, 2026

- Destination resolved: owner reconfirmed reverse-split Discord channel `1552850262966669352`, noting it had already been supplied previously. This supersedes the earlier missing-channel notes below. Recorded in operations for coordinator-controlled configuration; no hosted variable or delivery change. The disabled-worker approval and other activation gates remain separate.

- Owner directly confirmed the UI is approved and requested completion. Coordinator verified live main `30495ce7337959361fd4c562131509092b5c014b`, successful deployment `9388987e-d88f-4e79-9cd8-4092521f11d3`, one running instance, ready health and 126 migrations. Coordinator browser verification confirmed the private Help page and both links work. These are coordinator-reported hosted checks, not this task's independent production operations.
- Confirmed assigned checkout clean at the exact production parent before resuming source work. Existing owner gate, forced-empty preview, halt delivery and paused Welcome stay unchanged. Reverse-split channel requested from owner; data-use entitlement and delivery/preference activation remain open.
- Authored same-accession SEC document follow-up (bounded depth/count), explicit final-ratio/trading-date sentence forms and conditional-date withholding. Added durable rotating collection units and a recent/backfill source-queue rotation, plus one-at-a-time market snapshots using the latest completed regular session, including scheduled early closes. Provider failures no longer default security eligibility to true; coverage reports outstanding classification.
- Added standalone nightly/Sunday digest planning, dated snapshot selection, material-event revision signatures, changed-date/cancellation update messages and atomic digest snapshot/outbox creation. A prior unresolved event pauses its update rather than fabricating a cancellation. Morning replay of an unsent evening digest is prevented by expiry. No hosted hook invokes these modules.
- Added immutable ordered Discord parts, fenced leases, explicit no-mention payloads, guild/channel/bot validation, definitive-rate-limit retry, and exact bot/content/reference receipt reconciliation after an uncertain POST. No blind resend after uncertain delivery or process death. A receipt outside the latest 100 channel messages may remain unresolved; after 24 hours it becomes an explicit failed reconciliation rather than a duplicate post. This favors duplicate prevention, not an exactly-once or guaranteed-delivery claim.
- Refined only the still-unapplied 0142 source with nullable `first_attempt_at_utc`. Identity remains namespace `news`, migration `0142_news_reverse_split_alerts`, order `142`; canonical normalized SQL SHA-256 is `68111c1f33c058be186f55480cd00613277c3a64c3e6e456bcff8a8c382002b0`. Coordinator confirmed exclusive reservation, but manifest registration/application is not authorized by that ownership check. Paused Welcome0141 remains untouched.
- Coordinator cleared ownership of the exact disabled shared worker hookup, preserving all existing workers. The tool safety reviewer nevertheless rejected the combined new runtime/shared hook because it can activate collection/sending when enabled. No part of that rejected patch was applied; `runtime.ts` is absent and shared worker/manifest are unchanged. A direct owner question asks approval for the disabled hookup only. Do not bypass that gate through another tool or path.
- Authored focused source/market/message and outbox/transport cases, including lease expiry, duplicate suppression, uncertain POST recovery, wrong guild, mentions and rate limiting. These cases have NOT been executed under the current no-test-runner policy. Targeted TypeScript and lint checks are recorded below at checkpoint; no production build, local server, package installation, database initialization/application, provider request, subscription change or live send occurred during this implementation batch.
- Remaining work is explicit: owner response for disabled hookup; EODHD commercial data-use confirmation; coordinator-controlled final migration manifest/identity/activation; source and restart proof with approved verification; Push/email opt-in and existing-delivery integration; external Watchlist posting bridge if required; populated owner-view/real delivery acceptance. The destination channel is now recorded above. This is a source checkpoint, not feature completion or a release request. Approved UI, forced-empty owner preview, working halt alerts and all unrelated work remain preserved.
- Final source checks: targeted no-emit TypeScript over 19 roots and resolved dependencies passed with zero diagnostics; targeted ESLint over the same 19 module/migration files passed with zero findings (configuration emitted a missing-local-React auto-detection notice only); `git diff --check` passed. Both Node checks were serial with a 768 MB heap cap and used existing canonical dependencies without installation. Authored tests and migration SQL were not executed. The schema checksum above was calculated statically from the literal SQL using the repository's normalized-statement algorithm; it is not database-application proof.

## Private Help serialization correction — September 25, 2026

- Coordinator reports preview commit `6dfb5de7751fc76af9fd29d57d193eeadb8b4e37` deployed successfully as Railway `6afff80e-2bb8-460a-a68e-c5eb04334127`, with healthy exact 126-schema identity and original volume. Main owner page renders; owner API is empty; anonymous page/API gates verified by the coordinator. This task did not independently perform those hosted checks.
- Coordinator's rendered Help check identified a server/client function-prop serialization error (digest `1990388189`) at the two `DashboardSecondaryAction component={Link}` sites. Removed the function-valued props and unused import; existing MUI action renders an anchor from the same string `href`. Labels, destinations, content, theme and owner page/metadata gates are unchanged. No shared component, schema, configuration, provider or runtime changes.
- Targeted single-page TypeScript check plus resolved dependencies and diff whitespace check performed; no tests, local build/server or deployment. Coordinator retains corrected hosted-render and release verification.

## Owner-only review gate — September 24, 2026 Eastern

- Coordinator relayed direct owner authorization for an owner-account-only production preview and approved the additional narrow shell-filter/private-Help paths. Coordinator retains all deployment/migration/production authority; this task performs source work only.
- Reused the existing two-stable-Discord-subject owner contract unchanged. It resolves active linked Discord identities for the authenticated Platform user and rejects local development, arbitrary Premium/admin/workspace-owner status, absent/malformed configuration and failed reads. No identity values or secrets copied into code or documentation.
- Added a fail-closed gate to page and metadata, private Help and metadata, and Watchlist batch API before ticker parsing or data reads. Navigation reuses the existing server-derived owner-access boolean. Denied Watchlist reads show no badges/details and stop repeat requests for that ticker set.
- Removed only the newly added public Tools/Watchlist Help entries; existing public guides remain intact. The central Reverse Splits guide is retained solely for private `/reverse-splits/help`, with an explicit owner-preview notice.
- Read-only review identified that PWA navigation snapshots independently enumerate the unfiltered shared navigation. Coordinator approved one additional filter in `app/pwa/offline-projection-capture.tsx` to exclude Reverse Splits from offline navigation for all users. Existing route eligibility classifies `/reverse-splits` and `/reverse-splits/help` as not available, so neither content page is captured; no contract or service-worker change was needed. No offline records, caches or subscriptions cleared or changed.
- Added a server-only review lock that returns unavailable/empty data before News storage access regardless of environment flags. No fake rows, News migration, provider calls, collection hook, Discord/Push/email activation or Welcome change. This allows schema-compatible review on production 0140 without applying 0142.
- Targeted TypeScript covered 25 roots and resolved dependencies with zero diagnostics, no emit and a 768 MB heap cap. Targeted ESLint covered 26 files: two errors and four warnings, all independently reproduced from production parent `e938e82a` in unchanged shell effects/imports and the Watchlist unused helper; no new lint findings. The shell issues are two `react-hooks/set-state-in-effect` errors, two existing dependency warnings and an unused import; Watchlist has one unused-function warning. No rules suppressed or unrelated halt/Trade Drawer code changed. `git diff --check` passed. No tests, build, browser, provider calls, runtime server, migration or deployment performed. Visual and live owner/non-owner acceptance remain coordinator release checks; this does not complete the full feature's open items.

## Earlier scope and implementation history

- Owner approved nightly 19:00 Eastern posts, Sunday weekly overview, shareholder approvals, float (not shares outstanding), ratio, effective date and regular-session close.
- Coordinator assigned existing `release-workspace-boundary-4e22`, branch `codex/reverse-split-discord-20260924`, clean parent `e938e82a86a7305e33fae45c812cebd212f5d50e`. Paused Welcome in cdb0 is untouched.
- Coordinator reserved News migration 0142; exclude paused 0141 from this branch.
- Direct source/data preflight succeeded. No production operation, migration, message, test runner, server or installation performed.
- Channel ID and EODHD commercial data-use confirmation outstanding.
- Source/parser, storage, scheduler, delivery and verification are not yet complete.
- Owner expanded the intended product to a dedicated paginated dashboard containing the full tracked list and shareholder-approved unscheduled splits. Discord, Push and email will summarize upcoming splits and link back to the page. Dashboard layout approval and shared-path coordination are required before that new implementation scope.
- Automatic shareholder-approval processing must not depend on owner filing review. Current conservative parsing is not yet accepted as sufficient for that requirement.
- Reverse splits remain first priority. Dilution alerts are deferred until a separate planning discussion with the owner.
- Owner added Watchlist integration: show known approval/announced split status when a ticker is posted and expose the full available reverse-split information on its detailed ticker page. Both consume the same current event records; no independent scraper or extra posting-triggered alert. Scope recorded only; Watchlist code and UI remain unchanged pending shared-path coordination and owner layout review.
- Owner clarified Watchlist copy: show only "Reverse split approved" or "Reverse split announced" when verified; never display "not scheduled". Approval can be shown without a scheduled date. Show no label if neither approval nor announcement is verified. This supersedes the earlier proposed approved/unscheduled label; implementation is still pending.

## Local source checkpoint after owner Proceed

- Added shareholder-approval extraction separate from selected ratios and split-adjusted trading dates; retained unknown approval dates as unknown, added announced-without-trading-date state, authorization expiry extraction and deferred conflict outcomes. These conservative rules still require representative filing validation and automated related-document fallback; they are not a claim of complete approval coverage.
- Added shared ticker resolution, positive-only Watchlist status projection and deterministic pagination/filter primitives. Repeated old approvals do not intentionally downgrade confirmed terms; conflicting same-day terms are surfaced separately. No actual Watchlist or dashboard component changed.
- Nightly selection now excludes approval-only records; message construction links to the configured dashboard and removes not-scheduled wording. The final dashboard route and message layout remain unapproved/unwired.
- Authored reserved migration 0142 and a source repository with compressed documents, source/version deduplication, fenced leases, delayed retries, current observations, coverage counts and durable runtime checkpoints. Migration not registered or executed. Discord delivery table exists only in migration source; its sender is not implemented.
- Added bounded acquisition methods for Nasdaq, rolling recent SEC discovery, seven-day-window one-year backfill, and one-source processing, with a persisted request budget. No hosted worker registration and no runtime ingestion performed.
- Split ingestion enablement from Discord delivery configuration. No environment variables were changed. EODHD float/close acquisition retains the previously verified endpoint strategy.
- Authored focused parsing, status, conflict, date/ratio and pagination cases. Did not execute Vitest or any test suite, per owner policy. A narrowly scoped TypeScript compiler check covered 12 local TypeScript files including the authored cases and migration, with zero diagnostics; no emit, build, dependency install, database operation or local server.
- Owner layout approval requested: ticker search, paginated table, All / Approved / Announced / History filters, factual columns and positive-only Watchlist status. Next work: approval/correction coverage proof, complete event/history/freshness contract, scheduler/delivery and expanded UI/shared-service allowlist after review. Push/email preferences and delivery integration are not implemented.

## Approved UI implementation checkpoint

- Owner approved the proposed table/filter layout and Watchlist labels. Implementing without a local preview server or production action.
- Watchlist owner confirmed no current Platform path conflict and identified `live-watchlist-client.tsx` as the actual list/detail rendering and refresh boundary. Preserve its SSR authorization wrappers, crawler metadata, archive/deactivated behavior and all analysis/indicator cards. No edit to the owner's separate dirty checkout or external runtime.
- Expanded exact page/API/component/Help path allowlist submitted to the release coordinator before shared edits. UI will consume stored News records only, enforce the feature/data-use gates, use theme tokens and expose missing/partial coverage honestly.
- Coordinator approved the exact expanded local allowlist. Implemented `/reverse-splits` with ticker search, All / Approved / Announced / History filters, 25-row pagination, desktop table and stacked mobile entries. Added Stock Tools navigation and the associated Help guide. No placeholder market records or sample values are shown.
- Added a server-only, read-only projection and authenticated bounded Watchlist batch endpoint. Existing dashboard/Watchlist authorization still runs before data reads; provider credentials and filing text are not included in browser payloads. Disabled/data-use-unapproved configuration returns no market records. Source freshness/coverage is shown separately from filtered-empty results.
- Added one status hook per Watchlist list/detail page, rather than per ticker row. It refreshes only while visible, reacts to ticker-set changes/visibility, cancels requests on cleanup and clears labels on failed reads. Existing SSR pages, crawler metadata, Watchlist state/event/store contracts, publication and notification triggers remain unchanged. Both badges and the full detail card require a verified positive label; there is no not-scheduled label.
- Added current/history catalogue projection, bounded observation reads and stored market snapshot access. Market snapshots reuse the already-authored News runtime table; no fifth table or schema delta was needed. No provider requests occur from dashboard or Watchlist reads. The market-refresh writer and hosted scheduler remain unconnected.
- Reviewed against Next.js server/client boundaries and React hook/dependency/accessibility guidance. New components use shared MUI theme tokens with no page-local dark palette; rendered desktop/mobile and Light/Navy visual acceptance remains unverified because no preview server or hosted deployment was authorized.
- Targeted lint over 22 feature/shared files reported zero errors and one pre-existing unused-function warning (`cleanLevelMapCardBody`) in the unchanged Watchlist code, confirmed against the parent. Focused TypeScript initially identified three unsupported MUI Typography display props; those were replaced with `sx` properties. The final targeted compiler program covered 22 roots and their resolved dependencies with zero diagnostics, no emit, and a 768 MB heap cap. `git diff --check` passed. No tests, build, architecture runner, provider call, live send, migration or server start in this UI checkpoint.
- Help review required updates: new Reverse Splits guide plus an additive Watchlist explanation. These document approval vs final terms, float estimates, dated closes and absence/coverage semantics without changing Halt Alerts instructions.
- Remaining before feature completion: reliable automatic approval/related-filing extraction proof; correction/lifecycle/history cases; connected acquisition/market refresh; registered and verified migration; Discord/Push/email delivery and preferences; real UI and end-to-end evidence; owner-authorized coordinator release. This UI source checkpoint is not a production-ready handoff.
