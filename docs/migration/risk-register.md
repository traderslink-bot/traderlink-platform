# TraderLink Platform Replacement Risk Register

**Phase:** 1 - inventory and baseline  
**Status:** Open risks and required controls; no risk is permission to mutate the system  
**Scale:** Critical can expose/lose private data or publish incorrect financial facts; High can lose product behavior or block migration; Medium affects maintainability, clarity, or bounded operation.

| ID | Severity | Risk/evidence | Required control and gate | State |
| --- | --- | --- | --- | --- |
| R-001 | High | `v4-temp-sql` was mistakenly created inside the July 29 backup and could be confused with an active replacement database. No source/env reference uses it. | Preserve it as backup evidence, explicitly reject it as a migration source, and create a newly named replacement DB outside repositories in Phase 2. | Contained by owner decision |
| R-002 | Critical | The active configured private SQLite file is locked by a process and lacks an immutable Phase 1 hash. | Identify process ownership; use SQLite online backup at an authorized checkpoint; hash, count, restore-test, and freeze the migration snapshot. | Open |
| R-003 | High | Academy, News, Watchlist, affiliate, Level Analysis, rules, and Journal paths can converge on `TRADER_INTELLIGENCE_DB_PATH`. | Replacement configuration and repositories have explicit module ownership; no generic Journal-named fallback for peer modules. | Open |
| R-004 | Critical | Working SQLite files, backups, WAL/SHM, a structure dump, and logs are untracked inside the repository. | Preserve privately for migration, never stage; move only through approved backup/provenance procedure after reconciliation. | Contained by no-write rule |
| R-005 | High | 107 source files directly reference V3; default launcher and CI also depend on it. | Ordered decoupling with equivalent owner/access/data checks before each V3 removal; zero runtime references required for retirement. | Open |
| R-006 | Critical | V3 global authority/readiness can hide all dashboard data because one record/dependency is unresolved. | Metric-specific eligibility and coverage; pending chain only affects dependent round trip/metrics. | Architectural control accepted; not implemented |
| R-007 | High | Current local `main` has five tracked modifications, 32 untracked files, and is 70 commits ahead of origin. | Reconcile ownership and form an intentional checkpoint before Phase 2; no reset/clean/broad overwrite. | Open |
| R-008 | High | Seventeen canonical worktrees have commits not contained by current main. | Item-by-item reconcile/accept/reject/archive before removal. | Open |
| R-009 | High | `trader-intelligence-v2` has 25 tracked and 328 untracked files, dirty worktrees, unique branches, and 19 prunable metadata records. | Treat as protected legacy recovery workspace; inventory unique behavior/data before archive or prune. | Open |
| R-010 | Critical | Big Time Windows automation can pull, commit, push, merge, create worktrees, and deploy. Installed task/path is unknown because Windows denied enumeration. | Prove installed task configuration and update runner to the guarded deployment contract before cleanup or future scheduled execution. | Open |
| R-011 | Medium | Node PID 3160 serves port 3000 and used about 1.28 GB RAM, relevant to the user's low-resource concern. | Identify its repository/need; stop only at an authorized runtime checkpoint; avoid unnecessary simultaneous servers. | Open |
| R-012 | High | `docs/routes.md` and older architecture docs conflict with current route source. | Source route inventory governs; reconcile docs after owner accepts Phase 1. | Contained |
| R-013 | High | The modern dashboard and 52-page legacy `/intelligence` application coexist, risking lost unique behavior or duplicate future implementations. | Map every legacy capability to preserve/replace/defer/reject before route retirement. | Open |
| R-014 | Critical | Historical statements may be uploaded in arbitrary order and overlap/reimport. Upload-order grouping can corrupt trades. | Global owner/account/instrument/currency chronological rebuild; broker identity/idempotency; conflicts to Data Decisions. | Contract accepted; not implemented |
| R-015 | Critical | Historical corrections can change round-trip IDs and orphan notes/tags/reviews. | Stable logical identity plus alias/migration records; acceptance checks confirm attachments survive rebuild. | Open |
| R-016 | Critical | Current source types accept number or string for financial inputs; JavaScript float arithmetic could drift. | Replacement canonicalizes decimal strings/minor units at ingestion and uses exact arithmetic end-to-end. | Open |
| R-017 | Critical | Currency can be absent and current analytics may partition or aggregate inconsistently. | Require/decide currency per affected record; never sum currencies without explicit FX facts; coverage explains exclusions. | Open |
| R-018 | High | Commission/fee coverage varies by broker/row and net metrics can look exact when incomplete. | Gross and net contracts separated; normalized charge cost plus original evidence; net/fee metrics conditional with coverage. | Open |
| R-019 | Critical | Private owner mode is single-user/V3-coupled; replacement may weaken owner/account isolation while simplifying. | Platform server-derived scope and account authorization on every read/write/rebuild/export; negative isolation acceptance evidence. | Open |
| R-020 | Critical | Raw broker statements and account details could leak through logs, fixtures, APIs, telemetry, or external AI/provider calls. | Private storage, file validation/limits, safe errors, redacted logs, retention rules, no raw private data to telemetry/external services. | Open |
| R-021 | High | `/workspace` composition could regress into one module failure blanking the whole page. | Independent bounded module summaries with ready/empty/limited/unavailable states. | Contract proposed |
| R-022 | High | Market-data/Level Analysis coverage and price basis can be incomplete or provider-dependent. | Versioned as-of provenance and basis/window checks; execution P/L remains broker fact; unsupported market metrics unavailable. | Open |
| R-023 | Critical | Academy lesson slug/progress is production user data and can be lost during module/storage changes. | Preserve protected slugs/aliases and progress records; run Academy-specific acceptance at migration checkpoint. | Existing safeguard; migration open |
| R-024 | High | Removing V3 CI checks too early loses security/architecture safeguards; leaving them forever blocks the replacement. | One-for-one replacement acceptance mapping, then retire individual jobs after equivalent proof. | Open |
| R-025 | Medium | Trade Tracker multi-day entry UI is intentionally unresolved. Premature design can combine daily notes or mislead users. | Implement only data capability first; separate UI plan and repeated owner visual approval. | Deliberately deferred |
| R-026 | High | Levels System runtime auto-discovers sibling workspace folders. Cleanup can silently change provider/warehouse behavior. | Explicit configured provider/warehouse path before removing/reorganizing Levels System folders. | Open |
| R-027 | Medium | Two 1,757,184-byte pre-load backups have matching observed counts but different hashes. | Record both separately; do not deduplicate by name, size, or counts; establish lineage. | Contained |
| R-028 | Critical | 1,072 executions/336 trades are derived legacy counts, not source-statement reconciliation proof. | Rebuild from preserved statement evidence and reconcile executions, 334 closed, 2 open, decisions, charges, and coverage. | Open |
| R-029 | High | A later broker import may duplicate a manual Trade Tracker execution. | Data Decisions shows both provenance records and supports merge/supersede/keep-distinct without silent double count. | Contract accepted; not implemented |
| R-030 | High | Direct process stops are generally allowed by the owner, but stopping the active DB/server during inventory could change evidence or interrupt unrelated work. | Phase-specific authorization controls action; Phase 1 remains read-only. Identify target before any later stop. | Contained |
| R-031 | High | Big Time currently calls OpenAI's older chat-completions HTTP endpoint directly and carries deployment automation in the same runner. | Separate content generation from Git/deploy operations and review the integration before future scheduled use. | Open, outside Journal first slice |
| R-032 | Medium | Existing metrics use ambiguous terms such as position size, drawdown, giveback, and return. | Metric registry defines exact factual name/formula/unit/basis; realized-path metrics never masquerade as account equity/mark-to-market. | Catalog control proposed |
| R-033 | High | Cleanup by folder name could delete unique commits, dirty source, private data, provider warehouses, or deployment dependencies. | Exact per-target proof and owner approval; registered worktrees removed through Git only; inventory rerun after each batch. | Contained by cleanup plan |
| R-034 | High | The uncommitted Workspace overview repair may be mistaken for an accepted baseline. | Continue to label it in progress/unverified until focused route/data/browser acceptance is explicitly authorized and completed. | Contained |
| R-035 | High | One of several legacy/experimental dashboards could be mistaken for the owner's accepted final design. | The light Material UI dashboard with the complete left navigation is the visual baseline; any shell missing Trades, Calendar with week/month views, Analytics, Analytics Lab, or Trading Rules is reference-only. Require iterative owner visual approval for visible changes. | Baseline accepted; future UI checks open |

## Risk acceptance rule

Only the project owner may accept a residual Critical/High risk. “Owner accepted” means the specific residual behavior and evidence are recorded in the migration register; silence, an old commit, a rendered page, or a passing unit test is not acceptance.
