# Phase 7 Legacy Retirement Progress

**Status:** Non-destructive local retirement boundary established on
2026-08-02. Physical cleanup and hosted cutover remain explicit owner/external
operations.

**Acceptance evidence:** [Phase 6 Replacement Acceptance Report](phase-6-replacement-acceptance-report.md)

**Workspace control:** [Workspace and Worktree Cleanup Plan](workspace-and-worktree-cleanup-plan.md)

## Canonical direction

`C:\Users\jerac\Documents\TraderLink\traderlink-platform` is the canonical
local development application for the replacement product. New Platform,
Journal, Journal Analytics and dashboard work belongs there.

`C:\Users\jerac\Documents\TraderLink\traderslink.pro` is no longer an
ordinary Journal development or local-dashboard runtime. It remains the intact
legacy recovery/current-production reference until the hosted replacement is
published and accepted. Its V3 engine must not be revived as replacement
runtime authority.

## Retired locally

- Ordinary replacement routes do not enter the V3 runtime. The 146-file active
  guard is V3-free and all 52 preserved `/intelligence` filesystem pages have
  an explicit pre-filesystem disposition.
- Legacy execution/trade/tag/rule/note records are test data, not migration
  inputs. Their accepted replacement behaviors are implemented in the Platform
  database and were verified under two selected Journal accounts.
- Port 3010 is stopped. No legacy Node server is required to run the accepted
  replacement dashboard.
- `v4-temp-sql` remains an unused early experiment inside the July 29 backup.

## Preserved intentionally

- The legacy repository, its dirty/untracked evidence and all registered
  worktrees remain untouched.
- The legacy database, replacement database, final Phase 6 backup/restore pair,
  earlier backups, original private statement and evidence vault remain private
  and preserved.
- The current production website remains sourced from its already-published
  legacy commit until an authorized replacement deployment succeeds.
- Separate bots, Playwright projects, Levels System folders and the low-priority
  Big Time work remain outside this retirement action.

## External and owner-controlled remainder

- The Phase 6-accepted replacement source package is preserved locally at
  commit `b9575e2ed8ba93c23c3c4b8e35d80c26f71477c6`. The branch has no upstream,
  push or deployment; this local recovery checkpoint is not Git publication.
- The legacy direction marker is preserved locally at commit
  `6d659281e37e86dccaed3dfd66edae7ee4054766`. The two pre-existing legacy
  product modifications and 22 untracked preservation files remain untouched.
- Real hosted-source preview/transfer needs four credentials plus reviewed fresh
  source backups; none is currently available locally.
- Initial-owner Discord linking, Docker image execution, Railway resources,
  secrets, volume upload, deployment and DNS remain unexecuted.
- No folder, worktree, branch, database, backup or source file may be deleted
  without an exact refreshed inventory and explicit owner approval.

The user's preferred retirement result is preservation without confusion: the
old folder may remain indefinitely, but future Codex work must treat it as a
recovery/current-production reference rather than the active replacement app.
