"use client";
import Box from "@mui/material/Box";
import Button from "@mui/material/Button";
import Grid from "@mui/material/Grid";
import MenuItem from "@mui/material/MenuItem";
import Stack from "@mui/material/Stack";
import TextField from "@mui/material/TextField";
import Typography from "@mui/material/Typography";
import { DashboardPanel } from "@/app/dashboard-ui";
import type { TraderLinkCommunityDashboardSnapshot } from "@/src/modules/communities/contracts/traderlink-community-platform-contracts";
import { createCommunityCoachingPlanAction, saveCommunityCoachAction, setCoachingRelationshipStatusAction, updateCommunityCoachingPlanStatusAction } from "./community-actions";
import { CoachWorkspaceNavigation } from "./coach-workspace-navigation";

export function CoachSetupPage({snapshot,isReview=false}:{snapshot:TraderLinkCommunityDashboardSnapshot;isReview?:boolean}){const profile=snapshot.coaches.find(x=>x.userId===snapshot.viewer.userId);const plans=profile?snapshot.plans.filter(x=>x.coachProfileId===profile.coachProfileId):[];const requests=snapshot.relationships.filter(x=>x.coachUserId===snapshot.viewer.userId&&x.status==="pending");return <><CoachWorkspaceNavigation slug={snapshot.community.slug}/><Grid container spacing={2}><Grid size={{xs:12,lg:5}}><DashboardPanel title="Coach profile"><Box action={isReview?undefined:saveCommunityCoachAction} component="form"><input name="communitySlug" type="hidden" value={snapshot.community.slug}/><Stack spacing={1}><TextField defaultValue={profile?.displayName??snapshot.viewer.displayName} label="Coach name" name="displayName" required/><TextField defaultValue={profile?.headline} label="Headline" name="headline"/><TextField defaultValue={profile?.biography} label="About your coaching" minRows={3} multiline name="biography"/><TextField defaultValue={profile?.deliverySummary} label="How you coach" minRows={2} multiline name="deliverySummary"/><TextField defaultValue={profile?.capacity??10} label="Student capacity" name="capacity" type="number"/><Button disabled={isReview} type="submit" variant="contained">Save coach profile</Button></Stack></Box></DashboardPanel></Grid><Grid size={{xs:12,lg:7}}><DashboardPanel title="Enrollment requests"><Stack spacing={1}>{requests.map(item=><Stack direction="row" key={item.relationshipId} sx={{alignItems:"center",borderBottom:1,borderColor:"divider",justifyContent:"space-between",pb:1}}><Box><Typography style={{fontWeight:800}}>{item.studentDisplayName}</Typography><Typography color="text.secondary" variant="body2">{item.planName}</Typography></Box><Stack direction="row">{["active","declined"].map(status=><Box action={isReview?undefined:setCoachingRelationshipStatusAction} component="form" key={status}><input name="relationshipId" type="hidden" value={item.relationshipId}/><input name="status" type="hidden" value={status}/><Button disabled={isReview} type="submit">{status==="active"?"Accept":"Decline"}</Button></Box>)}</Stack></Stack>)}</Stack></DashboardPanel></Grid></Grid>{profile?<DashboardPanel title="Create coaching plan"><Box action={isReview?undefined:createCommunityCoachingPlanAction} component="form"><input name="communitySlug" type="hidden" value={snapshot.community.slug}/><input name="coachProfileId" type="hidden" value={profile.coachProfileId}/><Grid container spacing={2}><Grid size={{xs:12,md:4}}><TextField fullWidth label="Plan name" name="name" required/></Grid><Grid size={{xs:12,md:4}}><TextField defaultValue="monthly" fullWidth label="Delivery" name="cadence" select><MenuItem value="weekly">Weekly</MenuItem><MenuItem value="monthly">Monthly</MenuItem><MenuItem value="trade_reviews">Trade reviews</MenuItem><MenuItem value="custom">Custom</MenuItem></TextField></Grid><Grid size={{xs:12,md:4}}><TextField fullWidth label="Price shown" name="priceLabel"/></Grid><Grid size={{xs:12,md:6}}><TextField fullWidth label="What members receive" minRows={3} multiline name="description"/></Grid><Grid size={{xs:12,md:6}}><TextField fullWidth label="Payment and access instructions" minRows={3} multiline name="paymentInstructions"/></Grid><Grid size={{xs:12,md:6}}><TextField defaultValue={snapshot.audiences[0]?.audienceId} fullWidth label="Who can see this plan" name="audienceId" select>{snapshot.audiences.map(x=><MenuItem key={x.audienceId} value={x.audienceId}>{x.name}</MenuItem>)}</TextField></Grid><Grid size={{xs:12,md:3}}><TextField fullWidth label="Trade reviews" name="tradeReviewLimit" type="number"/></Grid><Grid size={{xs:12,md:3}}><Button disabled={isReview} fullWidth sx={{height:56}} type="submit" variant="contained">Publish plan</Button></Grid></Grid></Box></DashboardPanel>:null}<DashboardPanel title="Coaching plans"><Stack spacing={1}>{plans.map(plan=><Stack direction="row" key={plan.planId} sx={{alignItems:"center",borderBottom:1,borderColor:"divider",justifyContent:"space-between",pb:1}}><Box><Typography style={{fontWeight:800}}>{plan.name}</Typography><Typography color="text.secondary" variant="body2">{plan.cadence} · {plan.priceLabel}</Typography></Box><Box action={isReview?undefined:updateCommunityCoachingPlanStatusAction} component="form"><input name="communitySlug" type="hidden" value={snapshot.community.slug}/><input name="planId" type="hidden" value={plan.planId}/><Button disabled={isReview} name="status" type="submit" value={plan.status==="paused"?"active":"paused"}>{plan.status==="paused"?"Publish":"Pause"}</Button><Button color="error" disabled={isReview} name="status" type="submit" value="archived">Archive</Button></Box></Stack>)}</Stack></DashboardPanel></>}
