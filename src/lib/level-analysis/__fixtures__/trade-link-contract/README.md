Level analysis journal trade-link contract fixtures.

These compact fixtures lock the journal-side contract for attaching accepted
level-analysis delivery symbol summaries to saved trades. They intentionally do
not include raw source payloads; raw payload preservation remains owned by the
delivery persistence record fixtures.

Source references:

- Packaged delivery base fixture:
  `src/lib/level-analysis/__fixtures__/persistence-contract/delivery-record.accepted.compact.json`
- Old single-snapshot base fixture:
  `src/lib/level-analysis/__fixtures__/journal-connector-level-analysis-snapshot-v1.json`
